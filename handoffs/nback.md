# 別タブ用 — Nバック

あなたはサイト「すぐ・あそび」（フォルダ `sugu-asobu`）の **Nバックだけ** を作る。
他のアプリ（フラッシュ暗算・ナンプレ・クロスワード・懸賞・かんたん計算・逆唱・後出しじゃんけん）は触らない。

日本語・平易・1手ずつ。ユーザーにファイルを直接編集させない。
GitHub の作者メールは `ponnmayo15-a11y@users.noreply.github.com`（Gmailは書かない）。

## 作業場所

`C:\Users\user\Desktop\AI 社長\sugu-asobu`

中身は `apps/nback/`（なければ作る）。
玄関の枠はもうある（いまは準備中）。
遊べるようになったら `js/apps.js` の Nバックだけ `ready: true` と紹介文を直す。
ホームの `index.html` は触らない。共通の見た目は `css/common.css` を使う。
各 HTML の末尾に、他アプリと同じく `../../js/access-url.js` と `../../js/access.js` を読む。
戻りは「ホームに戻る」。

公開住所: https://ponnmayo15-a11y.github.io/sugu-asobu-app-public/
正本: このフォルダの `CLAUDE.md`

## 何を作るか

つぎつぎ出る刺激が、**N個前と同じなら押す** 遊び。
ブラウザだけ。ログインなし。記録はこの端末。

フラッシュ暗算ではない。足し算しない。数字の点滅で合計は出さない。

このアプリの客は、すぐ遊びたい人、短い集中を試したい人。
一回で意味がわかる。難しい言葉は使わない。
「ワーキングメモリ」「知能検査」は見出しにしない。

## 最初の版（おすすめ）

**位置だけ。** 3×3のマスのどれかが光る。N個前と同じ位置なら押す。
Nは 1 / 2 / 3。最初は 2。
せってい → 遊ぶ → 終わり（正解の数）。

スマホでマスとボタンが大きく、隠れない。
ホームから遊べる。絵（thumb）は、遊べるようになってからでよい。

最初の返信で、次を番号で確認してから作る（決まっていたら作ってよい）。

1. 位置だけ（3×3。おすすめ）
2. 文字だけ
3. 音と位置の両方（二重。あとでよい）

## やらないこと

- 「仕事ができる」「認知症が防げる」「知能検査」と書く
- フラッシュ暗算に近づける（合計・級・段・サバイバル）
- ゲーム中の広告（広告はホーム下だけ。このページには置かない）
- 課金、全国ランキング
- 他のアプリのファイルをいじる（`js/apps.js` の Nバック1行だけは可）
- `brain-tap` を丸コピーして複雑にする

## 終わったら

`LOG.md` に追記。ネットに出すコミットは、ユーザーが「上げて」と言ってから。
ホームで遊べるアプリに「あそべる」は出さない。使えないときだけ「準備中」。
